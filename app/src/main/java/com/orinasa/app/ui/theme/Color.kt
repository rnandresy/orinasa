package com.orinasa.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Palette principale ────────────────────────────────────────────────────────
val Primary        = Color(0xFF1A1A2E)   // Bleu nuit profond
val PrimaryVariant = Color(0xFF16213E)
val Secondary      = Color(0xFF0F3460)   // Bleu marine
val Accent         = Color(0xFF00B4D8)   // Bleu clair accent

// ── Statuts ───────────────────────────────────────────────────────────────────
val StatusPresent  = Color(0xFF22C55E)   // Vert
val StatusAbsent   = Color(0xFFEF4444)   // Rouge
val StatusLate     = Color(0xFFF59E0B)   // Orange
val StatusLeave    = Color(0xFF3B82F6)   // Bleu
val StatusHalfDay  = Color(0xFF8B5CF6)   // Violet

// ── Priorités annonces ────────────────────────────────────────────────────────
val PriorityNormal    = Color(0xFF6B7280)
val PriorityImportant = Color(0xFFF59E0B)
val PriorityUrgent    = Color(0xFFEF4444)

// ── Neutres ───────────────────────────────────────────────────────────────────
val Surface0   = Color(0xFFF8FAFC)
val Surface1   = Color(0xFFF1F5F9)
val Surface2   = Color(0xFFE2E8F0)
val Border     = Color(0xFFE2E8F0)
val TextPri    = Color(0xFF0F172A)
val TextSec    = Color(0xFF64748B)
val TextMuted  = Color(0xFF94A3B8)
val White      = Color(0xFFFFFFFF)
val Black      = Color(0xFF000000)